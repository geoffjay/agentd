# Notes

Mostly here for writing prompts or things while other things are running.

## General

- prefer dependencies in the root `Cargo.toml` file and using `dependency = { workspace = true }`

## Notify

perform the following actions:

- use axum version 0.8.6
- use directories version 6.0.0

## UI

- create-gpui-app
- use gpui with `gpui = { version = "*" }`
- use gpui-component version 0.3.1
- use gpui-router version 0.2.6
